{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "maddiehenniger/nf-imputation-pipeline params.samplesheet schema",
  "description": "Schema for the samplesheet used as input to maddiehenniger/nf-imputation-pipeline",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["sampleID", "samplePath", "sampleIndex"],
    "properties": {
      "sampleID": {
        "type": "string",
        "pattern": "^\\S+$",
        "errorMessage": "A user-provided unique sample ID must be provided and cannot contain spaces.",
        "meta": "sampleID"
      },
      "samplePath": {
        "type": "string",
        "pattern": "^\\S+$",
        "format": "file-path",
        "errorMessage": "A BCF/VCF(.gz) file path for samples must be provided, cannot contain spaces, and must have extension '.bcf' or '.vcf'/'vcf.gz'."
      },
      "sampleIndex": {
        "type": "string",
        "pattern": "^\\S+$",
        "format": "file-path",
        "errorMessage": "The paired indexed (CSI) file path for the associated sample must be provided, cannot contain spaces, and must match the sample file name with the extension '.csi'."
      },
      "pedigree": {
        "type": "string",
        "pattern": "^\\S+$",
        "format": "file-path",
        "errorMessage": "If providing the optional pedigree file associated with the test samples to be used for phasing, the file path to the pedigree file must be provided, cannot contain spaces, and must end with the extension '.fam' per SHAPEIT requirements."
      }
    }
  }
}